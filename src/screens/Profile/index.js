import { Text, View } from 'react-native';

import { styles } from './styles';

export const Profile = () => {
    return (
        <View>
            <Text>
                Profile
            </Text>
        </View>
    );
}