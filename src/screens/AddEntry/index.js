import { Text, View } from 'react-native';

import { styles } from './styles';

export const AddEntry = () => {
    return (
        <View>
            <Text>
                AddEntry
            </Text>
        </View>
    );
}